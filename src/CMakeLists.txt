INCLUDE_DIRECTORIES(${CMAKE_CURRENT_BINARY_DIR})
INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

FILE(GLOB_RECURSE THERMOCHIMICA_SOURCES ${CMAKE_CURRENT_SOURCE_DIR} "*.f90" "*.F90")

add_library( thermochimica_lib ${THERMOCHIMICA_SOURCES} )
